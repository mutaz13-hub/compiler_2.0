// Minimal client-side script for the generated site.
// This file is a companion asset - it is copied to output/ verbatim and
// is not analyzed or processed by the compiler in any way.

document.addEventListener("DOMContentLoaded", function () {
    console.log("Site loaded.");
});
